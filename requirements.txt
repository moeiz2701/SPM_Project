# Minimal dependencies for Vercel deployment (under 250MB limit)
# Heavy ML libraries excluded - not needed for API runtime

# API Framework
fastapi==0.104.1
pydantic==2.5.0

# Data Handling (lightweight)
python-dateutil==2.8.2

# Logging
python-json-logger==2.0.7
